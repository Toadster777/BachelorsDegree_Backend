'use strict';

/**
 * delivery-address controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::delivery-address.delivery-address');
